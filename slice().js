let text = "JavaScript";
let part = text.slice(4, 10);

console.log(part); // "Script"

//  Explanation:
//  1. slice(start, end) extracts part of a string from start index to before end index.
//  2. It does not change the original string.
//  3. If you skip the end, it takes till the end of the string